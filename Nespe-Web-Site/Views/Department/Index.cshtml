@model Nespe.Application.WebSite.Models.DepartmentListModel

@{
    ViewBag.Title = "Gestion des départements";
}

<h2>Gestion des départements</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="hovered">
    <tr>
        @*
        <th>
            @Html.DisplayNameFor(model => model.ItemSelected.Version)
        </th>
        *@
        <th>
            @Html.DisplayNameFor(model => model.ItemSelected.SID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemSelected.Name)
        </th>
        @*
        <th>
            @Html.DisplayNameFor(model => model.ItemSelected.Description)
        </th>
        *@
        <th>
            @Html.DisplayNameFor(model => model.ItemSelected.Entity)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model.ItemList) {
    <tr>
        @*
        <td>
            @Html.DisplayFor(modelItem => item.Version)
        </td>
        *@
        <td>
            @Html.DisplayFor(modelItem => item.SID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        @*
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        *@
        <td>
            @Html.DisplayFor(modelItem => item.Entity)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
