@model Nespe.Application.WebSite.Models.PersonDepartmentItemModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>PersonDepartment</legend>

        @Html.HiddenFor(model => model.ItemSelected.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.ItemSelected.Role)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ItemSelected.Role)
            @Html.ValidationMessageFor(model => model.ItemSelected.Role)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ItemSelected.Department)
        </div>
        <div class="editor-field">            
            <a href="@Url.Action("Details", "Department", new { id=Model.ItemSelected.Department.Id })">@Html.DisplayFor(model => model.ItemSelected.Department.Name)</a>
            @Html.ValidationMessageFor(model => model.ItemSelected.Department)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ItemSelected.Person)
        </div>
        <div class="editor-field">
            <a href="@Url.Action("Details", "Person", new { id=Model.ItemSelected.Person.Id })">@Html.DisplayFor(model => model.ItemSelected.Person.FullName)</a>
            @Html.ValidationMessageFor(model => model.ItemSelected.Person)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
