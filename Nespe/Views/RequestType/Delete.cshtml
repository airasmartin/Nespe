@model Nespe.Models.RequestTypeModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>RequestType</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Selected.Name)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Selected.Name)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Selected.Description)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Selected.Description)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
