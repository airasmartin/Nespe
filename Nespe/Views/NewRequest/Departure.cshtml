@model Nespe.Models.DepartureNewRequestModel
@using System.ComponentModel.DataAnnotations;
@using Nespe;
@using RequestKindEnum = Nespe.Models.RequestKindEnum;
    @Html.Partial("_TopPartialView")
        @using (Html.BeginForm())
        {
            <fieldset class="Form">
            <p class="Validation">@Html.ValidationSummary(false)</p>
            @Html.HiddenFor(model => model.Id)
            @Html.Partial("_DeparturePartialView")
                <p>
                    @Html.HiddenFor(requestType => requestType.kind)
                    <input type="submit" value="Soumettre Demande"/>
                </p>
            </fieldset>
        }
@Html.Partial("_BottomPartialView")